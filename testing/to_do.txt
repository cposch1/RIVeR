to consider:

when running as the new written py scripts or the previously provided nbs they need different config.py and setup.sh configuration. So if those are altered, the respectively other processing line is broken.

As of now, the config and setup is defined for running the py scripts



TO DO:

think about using cross-section selection only once a day

fixes for piv needed:
- output not confident
- issues with negative masks/rois
- all-NaN values issue

additions to last scripts
- overwrite function
- flag passing (default values)
- verbose option and date subsetting

make it possible to run on the server
